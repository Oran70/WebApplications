{
  "name": "oroots-gallery",
  "version": "1.0.0",
  "description": "ORoots Gallery - Plant Root Art Collection",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch backend/db.json --host 0.0.0.0 --port $PORT --static ./",
    "dev": "json-server --watch backend/db.json --port 3000 --static ./"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
